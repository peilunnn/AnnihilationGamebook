class Page {
    constructor(number, heading, content, choices) {
        this.number = number
        this.heading = heading
        this.content = content
        this.choices = choices
    }
}

export default Page